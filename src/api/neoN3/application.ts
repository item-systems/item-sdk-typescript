/**
 * Reserved namespace for future application-level invocation builders.
 *
 * The current SDK exposes the contract domains that are already stable in the
 * deployed ITEM contracts. This placeholder exists so the public module layout
 * can grow without forcing a breaking reorganization later.
 */
export class ApplicationAPI {
  // Intentionally empty until application-level contract operations are added.
}
