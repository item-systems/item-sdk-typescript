/**
 * Helper utilities used by the public SDK facade.
 *
 * These functions handle lower-level concerns such as stack parsing, polling,
 * byte conversions, and iterator materialization so that the main `Item` class
 * can present a cleaner domain-oriented API.
 */
export * from './utils'
